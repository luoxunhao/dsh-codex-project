# Standalone plugin workspace. deny-by-default build scripts: koffi is the
# native FFI layer of @deepseek-ai/dsh-sandbox-windows-acl (Win32 token/ACL
# APIs) and genuinely needs its install script.
allowBuilds:
  koffi: true
